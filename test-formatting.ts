// Test file with intentionally bad formatting
const badFormatting = "too many spaces"
const missing_semicolon = "this will pass because we don't use semicolons"
const goodFormatting = "this is fine"

export { badFormatting, goodFormatting }
